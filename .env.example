# Canonical site URL for SEO, Open Graph, and sitemap (no trailing slash).
# Production: https://codex-theme.pages.dev (also the build default when NODE_ENV=production).
NUXT_PUBLIC_SITE_URL=https://codex-theme.pages.dev
