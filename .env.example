# Canonical site URL for SEO, Open Graph, and sitemap (no trailing slash).
NUXT_PUBLIC_SITE_URL=

# GitHub repository used by /api/github and public links.
NUXT_PUBLIC_GITHUB_REPO_NAME=

# Server-only token to avoid GitHub API rate-limit 403 on deployment.
NUXT_GITHUB_TOKEN=
